{
  "name": "backend",
  "version": "1.0.0",
  "description": "Secure Firebase Authentication and Firestore backend for EcoSchedule",
  "main": "server.js",
  "type": "commonjs",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "seed": "node scripts/seedDatabase.js",
    "seed:clear": "node scripts/seedDatabase.js --clear",
    "seed:full": "node scripts/seedDatabase.js --clear --auth"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "firebase-admin": "^12.0.0"
  },
  "devDependencies": {
    "nodemon": "^3.1.0"
  }
}
